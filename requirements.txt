# ============================================
# ОБЯЗАТЕЛЬНЫЕ (базовый функционал realtime)
# ============================================

# API фреймворк - нужен для REST API и WebSocket endpoints
fastapi
uvicorn[standard]  # ASGI сервер для FastAPI
pydantic  # Валидация данных запросов/ответов

# HTTP клиенты - для запросов к внешним API кораблей
aiohttp  # Async HTTP клиент (рекомендуется для realtime)
httpx  # Альтернатива aiohttp, более современный API
requests  # Синхронный HTTP (если нужны синхронные запросы)

# WebSocket для realtime обновлений позиций кораблей
websockets  # Базовый WebSocket (если используете нативный WS)
python-socketio  # Socket.IO протокол (если фронтенд использует Socket.IO)

# База данных - хранение данных о кораблях, треках, истории
psycopg2-binary  # Драйвер PostgreSQL
sqlalchemy  # ORM для работы с БД
alembic  # Миграции БД

# Кэширование и pub/sub для realtime обновлений
redis  # Кэш позиций, pub/sub для WebSocket рассылки

# Работа с геоданными - расчет расстояний, маршрутов, зон
geopy  # Расчет расстояний между координатами, геокодирование
shapely  # Работа с геометрией (полигоны, точки, линии треков)
pyproj  # Проекции координат (если работаете с разными системами координат)

# Работа с датами и временем
python-dateutil  # Парсинг дат из разных форматов
pytz  # Работа с часовыми поясами

# Логирование
loguru  # Удобное логирование с контекстом

# ============================================
# ОПЦИОНАЛЬНЫЕ (зависит от требований)
# ============================================

# ============================================
# АНАЛИТИКА (расширенная аналитика данных)
# ============================================

# Обработка данных - анализ, агрегация, статистика
pandas  # Анализ данных, группировки, статистика по кораблям
numpy  # Математические операции, работа с массивами (зависимость pandas)
scipy  # Статистика, научные вычисления (если нужна продвинутая аналитика)

# Временные ряды - анализ истории перемещений кораблей
statsmodels  # Статистический анализ временных рядов, прогнозирование

# Визуализация - генерация карт и графиков (для работы БЕЗ фронтенда)
folium  # Генерация HTML карт с маркерами кораблей в реальном времени
plotly  # Интерактивные графики треков, статистики, временные ряды
matplotlib  # Статические графики (если нужны простые графики)
seaborn  # Красивые статистические графики поверх matplotlib

# HTML шаблоны - для генерации веб-страниц БЕЗ фронтенда
jinja2  # Шаблонизатор для генерации HTML страниц
python-multipart  # Для работы с формами в FastAPI

# Экспорт данных - выгрузка аналитики
openpyxl  # Экспорт в Excel (отчеты, статистика)

# Работа с XML/JSON - нужна если парсите XML данные от API
lxml  # Парсинг XML (если API кораблей отдает XML)
xmltodict  # Конвертация XML в словарь (удобнее чем lxml для простых случаев)

# Сериализация - нужна для оптимизации передачи данных
msgpack  # Бинарная сериализация (быстрее JSON, но нужна поддержка на фронтенде)

# Веб-скрапинг - нужна если нет API и нужно парсить сайты
selenium  # Автоматизация браузера для парсинга (если API недоступно)
webdriver_manager  # Автоматическое управление драйверами браузера

# Облачные сервисы - нужна если используете AWS
boto3  # AWS SDK (S3 для хранения данных, SQS для очередей)
apache-airflow-providers-amazon  # Только если используете Airflow для ETL

# Очереди сообщений - нужна для фоновой обработки задач
celery  # Асинхронные задачи (обработка данных, отправка уведомлений)
kombu  # Зависимость Celery

# Мониторинг - нужна для production мониторинга
prometheus-client  # Метрики для Prometheus (производительность, количество запросов)

# Криптография - нужна для безопасности
cryptography  # Шифрование данных, подписи (если храните чувствительные данные)

# ============================================
# РАБОТА БЕЗ ФРОНТЕНДА (Server-Side Rendering)
# ============================================
# FastAPI может генерировать HTML страницы напрямую:
# 1. Используйте Jinja2Templates для HTML шаблонов
# 2. Folium генерирует интерактивные карты в HTML
# 3. Plotly генерирует интерактивные графики в HTML
# 4. WebSocket работает через JavaScript встроенный в HTML
# 
# Пример структуры:
# - GET / - главная страница с картой (folium)
# - GET /analytics - страница аналитики (plotly графики)
# - GET /api/ships - JSON API для данных
# - WS /ws - WebSocket для realtime обновлений
# 
# Все это можно реализовать БЕЗ отдельного фронтенда!

python-dotenv